var arr = [1,2,3,4,5,6,7,8];
//arr[5] = 9; // manual system for insert a value
//arr.push(9); // insert value at the end
//arr.unshift(9); // insert value at the first
//arr.splice(3,0,9,12); // (index,delete,...insert)

//arr.pop(); //delete value at the end
//arr.shift(); //delete value at the first
//arr.splice(1,2); //(index,delete)

// arr.splice(2,1,4) // update

console.log(arr);